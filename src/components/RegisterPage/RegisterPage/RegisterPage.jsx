import React from 'react';
import RegisterFormContainer from '../RegisterContainer/RegisterFormContainer';

const RegisterPage = (props) => {
    return (
        <div>
            <RegisterFormContainer {...props}/>
        </div>
    )
};

export default RegisterPage;